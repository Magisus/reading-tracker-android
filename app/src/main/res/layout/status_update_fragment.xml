<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent">

    <TextView
        android:id="@+id/currentStatusText"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        android:paddingTop="3dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/changeStatusBtn"
        android:textColor="@color/link_text_material_dark"
        android:text="@string/change_status_link"
        android:textSize="12dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:layout_below="@id/currentStatusText"/>

    <TextView
        android:id="@+id/dateFinishedText"
        android:textColor="#FFFFFF"
        android:textSize="14sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:layout_below="@id/currentStatusText"
        android:layout_alignParentRight="true"/>

</RelativeLayout>